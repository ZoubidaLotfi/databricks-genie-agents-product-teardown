# Notebooks

Notebooks are optional supporting tools for synthetic-data generation, loading, validation, and evaluation. Keep notebook outputs reproducible, avoid credentials and production data, and link each notebook to its source tables and benchmark purpose.
